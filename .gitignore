# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
*.tmp

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# hongni server runtime + build artifacts
server/data/
server/tmp_test/
server/*.exe
server/*.test
server/*.out

# Android gradle build output (plugin AAR source project)
app/plugin-android/.gradle/
app/plugin-android/build/
app/plugin-android/plugin/build/
app/plugin-android/.kotlin/
app/plugin-android/local.properties

# Godot Android build template (generated by 'Project > Install Android Build Template')
app/android/
# Godot Android export output (apk + export logs)
app/.android/
